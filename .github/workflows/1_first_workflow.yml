# First workflow

name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greetings
        run: echo "Hello World!"
      - name: Print Goodbye
        run: |
          echo "Hello Dear User"
          echo "I am sending you goodbye not with multi comment"
          echo "Goodbye!"
        
